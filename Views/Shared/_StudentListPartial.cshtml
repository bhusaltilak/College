@model List<COLLEGE.Models.ViewModels.DetailsViewModel>

<<<<<<< HEAD



<table class="table table-bordered" style="width:100%">
    <thead class="table-dark">
        <tr>
            <th>Photo</th>
            <th>Name</th>
            <th>Address</th>
            <th>Grade</th>
            <th>ParentName</th>
            <th>Phone</th>
           <th >Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var s in Model)
        {
            <tr>
                <td>
                    @if (!string.IsNullOrEmpty(s.PhotoPath))
                    {
                        <img src="@s.PhotoPath" width="50" class="img-thumbnail" />
                    }
                </td>
                <td>@s.StudentName</td>
                <td>@s.Address</td>
                <td>@s.Grade</td>
                <td>@s.ParentName</td>
                <td>@s.Phone</td>


                <td>
                    <a asp-action="Edit" asp-route-id="@s.StudentId" class="btn btn-sm btn-warning">Edit</a>

                    <a asp-action="Details" asp-route-id="@s.StudentId" class="btn btn-sm btn-info">Details</a>

                    <form asp-action="Delete" asp-route-id="@s.StudentId" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this student?');">
                            Delete
                        </button>
                    </form>
                </td>

            </tr>

        }


    </tbody>

</table>
=======
@if (!Model.Any())
{
    <p>No student records available.</p>
}
else
{
    <div class="list-group">
        @foreach (var student in Model)
        {
            <div class="list-group-item list-group-item-action flex-column align-items-start mb-1 shadow-sm rounded">
                <div class="col-md-2">
                    <img src="@(string.IsNullOrEmpty(student.PhotoPath) ? "/Images/default-user.png" : student.PhotoPath)"
                         class="img-fluid rounded-start"
                         alt="Student Photo" />
                </div>
                <h5 class="mb-1">@student.StudentName</h5>
                <p class="mb-1"><strong>Grade:</strong> @student.Grade</p>
                <p class="mb-1"><strong>Address:</strong> @student.Address</p>
                <p class="mb-1"><strong>Parent Name:</strong> @student.ParentName</p>
                <p class="mb-1"><strong>Phone:</strong> @student.Phone</p>
               
                
            </div>
        }
    </div>
}
>>>>>>> 5572f4de444e80214a0e803bd53aa8e9f592e0f5
