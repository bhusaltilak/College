@model List<COLLEGE.Models.ViewModels.StudentFeeViewModel>

<h2>Student Fee List</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Student Name</th>
            <th>Grade</th>
            <th>Month</th>
            <th>Amount</th>
            <th>Amount Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var fee in Model)
        {
            <tr>
                <td>@fee.StudentName</td>
                <td>@fee.Grade</td>
                <td>@fee.Month</td>
                <td>@fee.Amount</td>
                <td>@fee.AmountStatus</td>
                <td>
                    <a asp-action="CreateEdit" asp-route-id="@fee.Id" class="btn btn-sm btn-warning">Edit</a>
//
                    <a asp-action="Details" asp-route-id="@fee.Id" class="btn btn-info btn-sm">Details</a>
=======
>>>>>>> 5572f4de444e80214a0e803bd53aa8e9f592e0f5

                    <form asp-action="Delete" asp-route-id="@fee.Id" method="post"
                          style="display:inline-block;"
                          onsubmit="return confirm('Are you sure you want to delete this entry?');">
                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }


    </tbody>

</table>
<a asp-action="CreateEdit" asp-controller="StudentFee" class="btn btn-success mb-3">
    Add Fee
</a>
